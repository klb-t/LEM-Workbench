# Real-only cleanup

This change removes production controls and screens whose end-to-end behavior did not yet exist, fixes the real Gemini generation serialization path, adds real embedding/generation endpoint checks, and adds CI/test enforcement for the implement-first policy.
